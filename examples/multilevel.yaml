routes:
  endpoint_one:
    proxy:
      url: http://localhost:8080
  endpoint_two:
    proxy:
      url: http://localhost:8080
  level_two:
    endpoint_three:
      proxy:
        url: http://localhost:8080
    endpoint_four:
      proxy:
        url: http://localhost:8080
    level_three:
      endpoint_five:
        proxy:
          url: http://localhost:8080
