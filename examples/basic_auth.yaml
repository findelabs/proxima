static_config:
  endpoint_one:
    url: http://localhost:8080
    authentication:
      basic:
        username: user
        password: testing
    security:
      client:
      - basic:
          username: user
          password: testing
