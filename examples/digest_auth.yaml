routes:
  endpoint_one:
    proxy:
      url: http://localhost:8080
      authentication:
        digest:
          username: user
          password: testing
      security:
        client:
          basic:
          - username: user
            password: testing
